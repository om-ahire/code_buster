#include <stdio.h>
int main()
{
    char str[100];
    int l = len(str);
    for (int i = 0; i < l; i++)
    {
        printf("Enter the string");
        scanf("%s", &str);
    }
    for (int i = 0; i < l; i++)
    {
        if (str[i] =)
    }
}