#include <stdio.h>
int main()
{
    char str[100];
    int l = len(str);
    for (int i = 0; i < l; i++)
        int count = 4;

    {
        printf("Enter the string");
        scanf("%s", &str);
    }
    for (int i = 0; i < l; i++)
    {
        if (len(str) > 4)
        {
            printf(str);
        }
    }
}
